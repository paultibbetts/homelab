apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-server-tls-cert
  namespace: argocd
spec:
  secretName: argocd-server-tls
  dnsNames:
    - argocd.lab.paultibbetts.uk
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production

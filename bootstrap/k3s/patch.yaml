---
- hosts: all
  become: true
  tasks:
    - name: Update apt repo cache
      ansible.builtin.apt:
        update_cache: yes
    - name: Upgrade all apt packages
      ansible.builtin.apt:
        upgrade: yes

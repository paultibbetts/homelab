version: "3.9"

services:
  nginx:
    image: nginx:latest
    container_name: nginx-test
    ports:
      - "8080:80"
    volumes:
      - ./static/index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped


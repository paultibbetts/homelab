---
- hosts: deskpi
  become: true
  roles:
     - community.beszel.hub
  tags:
    - beszel
    - beszel_hub
    - hub

- hosts: all,!artpi,!pangolin
  become: true
  roles:
    - community.beszel.agent
  vars:
    agent_public_key: "{{ vault_beszel_hub_public_key }}"
    agent_token: "{{ vault_beszel_agent_token }}"
    agent_hub_url: "{{ vault_beszel_agent_hub_url }}"
  tags:
    - beszel
    - beszel_agent
    - agent

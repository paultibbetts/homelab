---
mysql_databases:
  - name: freshrss
  - name: gitea

mysql_users:
  - name: freshrss
    host: 192.168.1.%
    password: "{{ vault_freshrss_database_password }}"
    priv: "freshrss.*:ALL"
  - name: gitea
    host: 192.168.1.211
    password: "{{ vault_gitea_database_password }}"
    priv: "gitea.*:ALL"

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ntfy-tls-cert
  namespace: ntfy
spec:
  secretName: ntfy-tls
  dnsNames:
    - ntfy.lab.paultibbetts.uk
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-dashboard-tls-cert
  namespace: traefik
spec:
  secretName: traefik-dashboard-tls
  dnsNames:
    - traefik.lab.paultibbetts.uk
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-staging

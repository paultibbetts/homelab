apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: longhorn-dashboard-tls-cert
  namespace: longhorn-system
spec:
  secretName: longhorn-dashboard-tls
  dnsNames:
    - longhorn.lab.paultibbetts.uk
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production

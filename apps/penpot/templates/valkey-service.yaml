apiVersion: v1
kind: Service
metadata:
  name: penpot-valkey
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: penpot
    app.kubernetes.io/component: valkey
spec:
  ports:
    - name: valkey
      port: 6379
      targetPort: valkey
  selector:
    app.kubernetes.io/name: penpot
    app.kubernetes.io/component: valkey

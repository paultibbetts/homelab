apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hortusfox-data
  namespace: hortusfox
  labels:
    app.kubernetes.io/name: hortusfox
    app.kubernetes.io/component: app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

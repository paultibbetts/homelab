apiVersion: v1
kind: Service
metadata:
  name: wallabag
  labels:
    app.kubernetes.io/name: wallabag
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wallabag
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

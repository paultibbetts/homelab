apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: healthchecks-tls-cert
  namespace: healthchecks
spec:
  secretName: healthchecks-tls
  dnsNames:
    - healthchecks.lab.paultibbetts.uk
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-staging


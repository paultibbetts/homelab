apiVersion: v1
kind: Service
metadata:
  name: gamja-backend
  labels:
    app.kubernetes.io/name: soju
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: soju
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

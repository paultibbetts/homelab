apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: soju-data
  labels:
    app.kubernetes.io/name: soju
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

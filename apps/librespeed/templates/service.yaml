apiVersion: v1
kind: Service
metadata:
  labels:
    app: librespeed
  name: librespeed
spec:
  type: ClusterIP
  selector:
    app: librespeed
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080


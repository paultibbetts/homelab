# Yamtrack API

This is an independent Yamtrack instance using the upstream `api` image tag at
`https://yamtrack-api.lab.paultibbetts.uk`. It has separate SQLite and Redis
storage from `apps/yamtrack`.

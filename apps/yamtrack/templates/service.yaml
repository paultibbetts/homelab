apiVersion: v1
kind: Service
metadata:
  name: yamtrack
  labels:
    app.kubernetes.io/name: yamtrack
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: yamtrack
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
